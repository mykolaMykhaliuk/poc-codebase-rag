# Embedding API Configuration
# Required: Your OpenAI API key (or compatible embedding API key)
EMBEDDING_API_KEY=sk-your-api-key-here

# Optional: Override the embedding API base URL (default: https://api.openai.com/v1)
# Use this for Azure OpenAI, Ollama, or other compatible APIs
# EMBEDDING_BASE_URL=https://api.openai.com/v1

# Optional: Override the embedding model (default: text-embedding-3-small)
# EMBEDDING_MODEL=text-embedding-3-small

# Codebase Configuration
# Path to the codebase you want to index (default: ./sample-codebase)
CODEBASE_PATH=./sample-codebase
